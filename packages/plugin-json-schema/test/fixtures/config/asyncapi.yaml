asyncapi: 3.1.0
info:
  title: JSON Schema config integration
  version: 1.0.0
components:
  schemas:
    Event:
      type: object
      required:
        - id
      properties:
        id:
          type: string
        sequence:
          type: integer
          minimum: 0
