asyncapi: 3.0.0
info:
  title: Core 3.0 fixture
  version: 1.0.0
channels:
  events:
    address: events
    messages:
      Event:
        payload:
          type: object
          properties:
            id:
              type: string
operations:
  sendEvent:
    action: send
    channel:
      $ref: "#/channels/events"
    messages:
      - $ref: "#/channels/events/messages/Event"
